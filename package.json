{
    "name": "@enwindui/monorepo",
    "private": "true",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "dev": "lerna run dev",
        "build": "lerna run build",
        "version": "lerna version --no-private"
    },
    "devDependencies": {
        "lerna": "8.1.9",
        "typescript": "^5.6.3"
    },
    "dependencies": {
        "highlight.js": "^11.10.0"
    }
}
